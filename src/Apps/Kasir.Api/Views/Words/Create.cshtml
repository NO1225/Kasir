@model Kasir.Api.ViewModel.CreateWordViewModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}

<h1>Create</h1>

<h4>CreateWordViewModel</h4>
<hr />
<div>
    <form asp-action="Create" class="row" enctype="multipart/form-data">
        <div class="col-md-4">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Information" class="control-label"></label>
                <input asp-for="Information" class="form-control" />
                <span asp-validation-for="Information" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WordImage" class="control-label"></label>
                <input asp-for="WordImage" accept="image/*" class="form-control" />
                <span asp-validation-for="WordImage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </div>
        <div class="col-md-4">
            @for (int i = 0; i < ((List<LanguageDto>)ViewBag.Languages).Count(); i++)
            {
                LanguageDto lang = ViewBag.Languages[i];
                <div class="form-group">
                    <input type="hidden"
                       name="WordLanguageDtos[@i].LanguageId"
                       value="@lang.Id" />
                </div>
                <label class="control-label">@lang.Name</label>

                <div class="form-group">
                    <label class="control-label">Name</label>
                    <input name="WordLanguageDtos[@i].Name" class="form-control" />
                </div>
                <div class="form-group">
                    <label class="control-label">Information</label>
                    <input name="WordLanguageDtos[@i].Information" class="form-control" />
                </div>
            }
        </div>
        <div class="col-md-4">
            @for (int i = 0; i < ((List<CountryDto>)ViewBag.Countries).Count(); i++)
            {
                CountryDto country = ViewBag.Countries[i];
                <div class="form-group">
                    <input type="hidden"
                       name="WordImageDtos[@i].CountryId"
                       value="@country.Id" />
                </div>
                <div class="form-group">
                    <label class="control-label">@country.Name</label>
                    <input type="file" name="WordImageDtos[@i].WordImage" accept="image/*" class="form-control" />
                </div>
            }
        </div>
    </form>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
